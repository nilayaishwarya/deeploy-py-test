version: 2
updates:

  # Maintain dependencies for pip in client
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"

  # Maintain dependencies for Docker ci
  - package-ecosystem: "docker"
    directory: "/ci/docker"
    schedule:
      interval: "daily"
